manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
    # --- 以下のリモートを追加 ---
    - name: badjeff
      url-base: https://github.com/badjeff
  projects:
    - name: zmk
      remote: zmkfirmware
      revision: main
      import: app/west.yml
    # --- 以下のプロジェクトを追加 ---
    - name: zmk-pmw3610-driver
      remote: badjeff
      revision: main
  self:
    path: config
